# Dotfiles

Mac setup scripts and config files.

## Notes
- Install apps via Homebrew where available
- Scripts should be idempotent (safe to rerun)
- API keys/secrets go in `~/.zshrc.local` (not synced, not in git)
- Ask clarifying questions if anything is ambiguous, don't assume or pick silently
